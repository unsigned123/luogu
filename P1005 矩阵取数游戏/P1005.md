本题关键：高精度，这可以作为模板题，和高精度混个脸熟！
高精度坑不少！

1.注意大base下，单精度乘以多精度时，某一位潜在的long long溢出问题
2.注意高精度下base不为10对print的影响，此时最高位直接输出，其他位补0;
还必须注意：base不为10对big_num(integer)构造函数的影响
3.注意高精度必须拥有自己的比较函数，如果贸然用max(a,b)实际上是在比较指针地址高低！！


TODO: 此外，必须使用普通DP再做一次，不能只会记忆化递归！
注意->这种区间DP的数组DP做法是和递归反过来的——是从数组只有一个元素开始。

**区间DP标准做法：先枚举完所有的长度为1的区间，然后是2的，3的……（就是以区间长度为第一个循环变量）**
**区间DP的循环变量并不直接作为f[][]的参数！**
**区间增长的部分既可以视作最先取，也可视为最后取，二者等效（要检查合法性），哪个方便用哪个，这里是视作最先取更方便，这意味着，所有length=1的区间都是最后一次取得的，所有length=2都是倒数第二次取得的，以此类推**
**列表DP注意循环的边界条件！**

->以后注意：数组DP和递归DP的递推方向是相反的！！
4.注意区间DP问题的数组DP做法
5.必须注意到各行互相独立的特征，从而“化横为纵”
6.注意这个预先储存2的次幂的办法